"use strict"; //treat all js code as newer version, now by default it is taken, not requires to mention this.

//alert(3+3) // will run on browser but not in nodejs

/*
premitive data type:
number -> range 2^53
bigint
string -> ""
boolean -> true/false
null -> standalone value
undefined -> when variable is not defined with any value
symbol -> unique
*/

/*
object
*/

console.log(typeof undefined); //undefined
console.log(typeof null); //object
